<%= Like.where(:story_id => @story).count %> Likes
<br/>
<% @like = Like.where(:story_id => @story, :user_id => current_user.id).first %>
<%= link_to "Unlike", like_path(@like), :method => :delete, :remote => true, class: "btn btn-small" %>